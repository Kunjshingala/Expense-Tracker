{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "transactions": {
          "all-transactions": {
            ".indexOn": "time"
          }
        }
      }
    }
  }
}
